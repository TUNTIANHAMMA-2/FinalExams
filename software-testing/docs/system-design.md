# 测试策略与用例设计

## 1. 测试目标

1. 确认 RhizoDelta 核心功能可用、业务流程正常。
2. 确认接口的入参校验、鉴权约束与错误处理符合预期。
3. 发现并记录缺陷，给出系统质量结论与上线建议。

## 2. 测试类型

| 类型 | 说明 | 本次 |
| --- | --- | --- |
| 黑盒功能测试 | 以用户/接口视角验证输入→输出对应关系 | 主体 |
| 接口测试 | Postman 对 `/api/**` 直接发请求并断言 | 专项 |
| 软件审查 | 评审需求/用例 | 辅助 |
| 性能 / UI 自动化 | JMeter / Selenium | 三选一中**未选**，不展开 |

## 3. 用例设计方法（对应总结报告 4.1）

- **等价类划分**：把 username、password、access token、refresh token、分页参数、UUID 划分为有效/无效输入域，并在 `2-测试用例.xls` 的“等价类划分”工作表中列出代表用例。
- **边界值分析**：围绕空用户名、空密码、密码 8 位最小有效边界、动态流最小页大小、负页码、UUID 格式边界取值设计用例。
- **错误推测**：缺字段、空值、重复注册、伪造 token、登出后旧 token、非法分页、无 token 访问受保护资源。
- **场景法**：注册→登录→查询资料→刷新令牌→查询图谱→登出→旧 token 失效的完整业务链。
- **因果图**：登录的"用户名/密码/账号状态"组合与结果。

## 4. 工具选型（接口测试专项）

- 选 **Postman**：直接对运行中的后端 `http://localhost:8090` 发请求；用环境变量管理 `baseUrl` 与登录后的 `token`；用 Tests 脚本断言状态码、`ApiResponse` 结构和业务字段；最终导出 Collection Runner / Newman 测试报告。
- 详见 [../deliverables/4-接口测试-Postman.md](../deliverables/4-接口测试-Postman.md)。

## 5. 测试环境

| 项 | 配置 |
| --- | --- |
| 被测后端 | RhizoDelta，Spring Boot，`http://localhost:8090` |
| 依赖 | Neo4j、Redis、RabbitMQ（本地） |
| 浏览器 | Chrome 最新版 |
| 接口工具 | Postman（最新版） |
| 操作系统 | Windows 10 / Linux |
| 取证 | 系统截图，存 `../generated/` |

## 6. 缺陷分级

沿用模板：严重 / 很高 / 高 / 中 / 低，详见 [../deliverables/3-Bug缺陷报告清单.xls](../deliverables/3-Bug缺陷报告清单.xls)。

## 7. 通过准则

- 本轮 20 条已实测核心子集用例执行率 100%，无"严重/很高"级未关闭缺陷；接口用例断言全部通过，已知缺陷以观察断言和缺陷清单记录，不阻断验收版报告；8 条补充设计用例作为后续回归扩展，不计入本轮通过率。
